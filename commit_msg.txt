fix(bridge): resolve Pier 6 geometry NameError and truncation

- Fix `NameError` by replacing remaining `BRIDGE_EAST_PIVOT_X` usages in `bridge.py` with `PIER6_X` and `PIER6_WEST_X`/`PIER6_EAST_X`.
- Update deck, parapet, and railing tube geometry to truncate evenly at Pier 6's west and east faces, eliminating overlaps with the rotated pier body.
- Remove obsolete triangular deck notch and related logic (`PIER6_NOTCH_LEN`, etc.) now that deck truncation is handled by simple segment splits.
- Remove vis-hint brushes that were intersecting the rotated pier body.
- Clean up unused imports (`math`, `STREET_SURFACE_T`, etc.) in `bridge.py` and remove `PIER6_NOTCH_LEN` from constants.
- Verified with map generation, regression tests (55/55 passed), and Ruff lint check.

Co-authored-by: Augment <augment@augmentcode.com>
